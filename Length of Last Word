class Solution {
    public int lengthOfLastWord(String s) {
        
        String[] list = s.split(" ");
        
        if(list.length == 0){
            return 0;
        }
        
        int lengthOfLastWord = list[list.length - 1].length();
        
        return lengthOfLastWord;
        
    }
}
